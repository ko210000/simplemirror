# 仓库内文本统一存 LF，避免 Windows 检出成 CRLF 后 gradlew 无法执行
* text=auto eol=lf

# 二进制文件不做换行符转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.jar binary
*.apk binary
*.aab binary
*.keystore binary
*.jks binary
